version: "3.1"

intents:
  - out_of_scope
  - bye

responses:
  utter_bye:
  - text: "Goodbye!"
actions:
  - action_gpt3_fallback

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
